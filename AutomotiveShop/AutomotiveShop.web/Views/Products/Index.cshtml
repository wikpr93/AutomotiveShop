@model IEnumerable<AutomotiveShop.model.Product>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista produktów</h2>

<p>
    @Html.ActionLink("Utwórz nowy", "Create", null, new { @class = "btn btn-primary btn-xs" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subcategory.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subcategory.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { productId=item.ProductId }, new { @class = "btn btn-primary btn-xs" }) |
            @Html.ActionLink("Szczegóły", "Details", new { productId = item.ProductId }, new { @class = "btn btn-primary btn-xs" }) |
            @Html.ActionLink("Usuń", "Delete", new { productId = item.ProductId }, new { @class = "btn btn-primary btn-xs" })
        </td>
    </tr>
}

</table>
